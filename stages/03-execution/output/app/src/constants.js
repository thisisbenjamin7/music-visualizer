export const COLORS = {
  primary: '#3b82f6',     // blue — bars, wave, circular rings
  secondary: '#f97316',   // orange — accents, beat highlights
  background: '#0a0a2a',  // dark midnight blue — canvas background
};

export const FONTS = {
  ui: '"MS Sans Serif", "Tahoma", sans-serif',
  size: 12,
};

export const TIMING = {
  pollInterval: 1000,  // ms between Spotify API polls
  smoothing: 0.8,      // animation value smoothing factor (0–1)
  bpm: 120,            // default simulated BPM for particle beat triggers
};
